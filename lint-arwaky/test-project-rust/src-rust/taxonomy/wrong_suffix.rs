// This is a taxonomy file
// But its name does not end with any allowed suffix (_vo, _entity, _error, _event, _bridge)
// Also, it contains less than 10 lines of code which violates AES005 file too short!

pub struct WrongSuffixData {
    pub value: String,
}
